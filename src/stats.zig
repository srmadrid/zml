pub const Uniform = @import("stats/uniform.zig").Uniform;
pub const Normal = @import("stats/normal.zig").Normal;
pub const Poisson = @import("stats/poisson.zig").Poisson;

// Array operations
// mean (over whole array, or specific axes if array is static)
// meanAlloc (over specific axes)
// meanInto

// var (over whole array, or specific axes if array is static)
// varAlloc (over specific axes)
// varInto

// std (over whole array, or specific axes if array is static)
// varAlloc (over specific axes)
// varInto
